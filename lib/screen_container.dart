import 'package:flutter/material.dart';

class ScreenContainer extends StatelessWidget {
  @override
  Widget build(context) {
    return Container(
      child: Column(
        children: [Text("Hello"), Text("Ankit")],
      ),
    );
  }
}
